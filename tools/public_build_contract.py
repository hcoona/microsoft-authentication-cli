PCACACHE_TEST_FILTER = (
    "FullyQualifiedName!~Microsoft.Authentication.MSALWrapper.Test.PCACacheTest"
)
PCACACHE_TEST_LIMITATION = (
    "PCACacheTest was excluded because it performs real platform-persistence "
    "validation; this result does not establish an unfiltered upstream test-suite "
    "pass or PCACache/keyring/Keychain/DPAPI behavior."
)
