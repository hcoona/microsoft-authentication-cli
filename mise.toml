[tools]
actionlint = "1.7.12"
gitleaks = "8.30.1"
hk = "1.56.1"
lychee = "0.24.2"
"pipx:check-jsonschema" = "0.38.0"
python = "3.13.15"
uv = "0.12.7"

[env]
HK_MISE = "1"

[tasks.check]
description = "Run the complete repository check set"
run = "hk check --all"

[tasks."check:fast"]
description = "Run the local fast check set"
run = "hk run pre-commit"

[tasks."hooks:install"]
description = "Install repository-local Git hooks through mise"
run = "hk install --mise"
